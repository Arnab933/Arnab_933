package InterfaceConcept;
// package Interface_conpet.java;

public class Interface_concept1 {
    /*
     * Interface's are like a class only but not completely.So we can't crate object
     * of the interface
     * that's reason we can't create instance variable that's for we have to declare
     * those variables as static because static variables are not depend on object. interface contain
     * abstract method.
     */

    /*
     * In java we can't extends multiple classes that's for java does not supports
     * multiple inheritance.Thats
     * reason we have to learn interfaces
     */

}
