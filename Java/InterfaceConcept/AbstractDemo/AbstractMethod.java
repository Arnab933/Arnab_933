package InterfaceConcept.AbstractDemo;
public class AbstractMethod {
    /*Abstract method is a method that's have only the definition but not implementation.It means we have
     * to override that method by implementing.A class that contains atleast an abstract method that's type
     * of class is called Abstract class.Note that if we implements an abstract class then we must have to
     * override that's abstract method.
     */
    //syntax:
    //  abstract void carrer(String name);  this is the general syntax of an abstract method.
}
